export class ParentCardModel {
    image: string;
    title: string;
    desc: string;

    constructor(image: string, title: string, desc: string) {
        this.image = image;
        this.title = title;
        this.desc = desc;
    }
}